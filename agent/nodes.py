def extract_issues_from_reviews(reviews: list[str]) -> list[str]:
    """
    Takes a list of review texts and returns atomic issues / requests / feedback.
    This will later become an LLM-powered node.
    """
    pass
